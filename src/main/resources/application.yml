spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/enroll?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 8ur266

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create     # 추후 validate 옵션으로 변경
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug